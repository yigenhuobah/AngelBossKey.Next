<Window x:Class="AngelBossKey.Next.App.WindowPickerDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="添加目标程序" Width="760" Height="560" MinWidth="620" MinHeight="420"
        WindowStartupLocation="CenterOwner" ShowInTaskbar="False">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <StackPanel>
                <TextBlock Text="选择当前窗口" FontSize="18" FontWeight="SemiBold" />
                <TextBlock Text="当前可用窗口" Margin="0,5,0,0"
                           Foreground="{StaticResource SecondaryTextBrush}" FontSize="12" />
            </StackPanel>
            <Button Grid.Column="1" Style="{StaticResource IconButton}" Content="&#xE72C;"
                    AutomationProperties.Name="刷新窗口列表"
                    ToolTip="刷新窗口列表" Click="Refresh_Click" />
        </Grid>

        <Grid Grid.Row="1" Margin="0,18,0,12">
            <TextBox x:Name="SearchBox" Height="38" Padding="38,8,12,8"
                     BorderBrush="{StaticResource BorderBrush}" BorderThickness="1"
                     TextChanged="SearchBox_TextChanged" />
            <TextBlock FontFamily="Segoe Fluent Icons" Text="&#xE721;" Margin="12,0,0,0"
                       VerticalAlignment="Center" Foreground="{StaticResource SecondaryTextBrush}"
                       IsHitTestVisible="False" />
        </Grid>

        <Border Grid.Row="2" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1"
                Background="{StaticResource SurfaceBrush}" CornerRadius="6">
            <DataGrid x:Name="WindowGrid" AutoGenerateColumns="False" HeadersVisibility="Column"
                      BorderThickness="0" GridLinesVisibility="Horizontal"
                      HorizontalGridLinesBrush="{StaticResource BorderBrush}" RowHeight="58"
                      SelectionMode="Extended" SelectionUnit="FullRow" IsReadOnly="True"
                      MouseDoubleClick="WindowGrid_MouseDoubleClick">
                <DataGrid.Resources>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Height" Value="38" />
                        <Setter Property="Background" Value="#F8FAFA" />
                        <Setter Property="Foreground" Value="{StaticResource SecondaryTextBrush}" />
                        <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}" />
                        <Setter Property="Padding" Value="12,0" />
                    </Style>
                    <Style TargetType="DataGridCell">
                        <Setter Property="BorderThickness" Value="0" />
                        <Setter Property="Padding" Value="12,0" />
                        <Setter Property="VerticalContentAlignment" Value="Center" />
                    </Style>
                </DataGrid.Resources>
                <DataGrid.Columns>
                    <DataGridTemplateColumn Header="程序" Width="200" SortMemberPath="Window.DisplayName">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <Image Source="{Binding Icon}" Width="30" Height="30" Margin="0,0,12,0" />
                                    <TextBlock Text="{Binding Window.DisplayName}" VerticalAlignment="Center"
                                               FontWeight="SemiBold" TextTrimming="CharacterEllipsis" />
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTextColumn Header="当前窗口" Binding="{Binding Window.Title}" Width="*"
                                        SortMemberPath="Window.Title" />
                    <DataGridTextColumn Header="路径" Binding="{Binding Window.ExecutablePath}" Width="230"
                                        SortMemberPath="Window.ExecutablePath" />
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <Grid Grid.Row="3" Margin="0,16,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock x:Name="CountText" VerticalAlignment="Center"
                       Foreground="{StaticResource SecondaryTextBrush}" />
            <Button Grid.Column="1" Style="{StaticResource SecondaryButton}" Content="取消" Width="82"
                    Margin="0,0,10,0" IsCancel="True" />
            <Button Grid.Column="2" Style="{StaticResource PrimaryButton}" Content="添加" Width="82"
                    Click="Add_Click" />
        </Grid>
    </Grid>
</Window>
